#include <stdio.h>

int main()
{
    int x;
    printf("Enter the number: \n");
    scanf("%d\n", x);
    if (x % 2 == 0)
        printf("The entered number is even\n", );
    else
        printf("The entered number is odd\n", );
}
